import { connect } from 'react-redux';
import DMSettings from './dm_settings';

const msp = (state, ownProps) => ({

})

const mdp = (dispatch) => ({

})

export default connect(msp, mdp)(DMSettings);