import { connect } from 'react-redux';
import DMDashboard from './dm_dashboard';

const msp = (state, ownProps) => ({

})

const mdp = (dispatch) => ({

})

export default connect(msp, mdp)(DMDashboard);