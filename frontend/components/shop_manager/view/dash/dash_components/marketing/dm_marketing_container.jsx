import { connect } from 'react-redux';
import DMMarketing from './dm_marketing';

const msp = (state, ownProps) => ({

})

const mdp = (dispatch) => ({

})

export default connect(msp, mdp)(DMMarketing);