import { connect } from 'react-redux';
import DMFinances from './dm_finances';

const msp = (state, ownProps) => ({

})

const mdp = (dispatch) => ({

})

export default connect(msp, mdp)(DMFinances);