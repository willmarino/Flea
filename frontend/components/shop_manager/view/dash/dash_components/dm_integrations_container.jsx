import { connect } from 'react-redux';
import DMIntegrations from './dm_integrations';

const msp = (state, ownProps) => ({

})

const mdp = (dispatch) => ({

})

export default connect(msp, mdp)(DMIntegrations);