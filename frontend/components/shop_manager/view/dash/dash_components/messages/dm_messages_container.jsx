import { connect } from 'react-redux';
import DMMessages from './dm_messages';

const msp = (state, ownProps) => ({

})

const mdp = (dispatch) => ({

})

export default connect(msp, mdp)(DMMessages);