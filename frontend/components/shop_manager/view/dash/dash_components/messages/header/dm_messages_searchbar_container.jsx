import { connect } from 'react-redux';
import DMMessagesSearchbar from './dm_messages_searchbar';

const mdp = (dispatch) => ({
// search func
})

export default connect(null, mdp)(DMMessagesSearchbar);