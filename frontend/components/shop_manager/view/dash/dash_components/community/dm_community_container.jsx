import { connect } from 'react-redux';
import DMCommunity from './dm_community';

const msp = (state, ownProps) => ({

})

const mdp = (dispatch) => ({

})

export default connect(msp, mdp)(DMCommunity);